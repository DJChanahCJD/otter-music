---
alwaysApply: true
---
OtterMusic 是一个基于 React + Vite + Capacitor 的移动端音乐应用，要求移动端优先。
* 组件：通过 `npx shadcn@latest add button` 等命令添加
* 图标：lucide-react按需导入
* 批量处理：import { processBatchCPU, processBatchIO } from "@/lib/utils";